# 品牌资产库网站 · 维护说明

面向 `ABOUT_<品牌>_v<版本>.md` 报告汇总的静态网站（画廊 / 列表 / 分类看板三种视图 + 两级搜索）。
网站规则单独记录在此文件，暂不写入《品牌解读指南》。

## 站点结构

| 路径 | 说明 |
|---|---|
| `index.html` | 首页与看板同页：液态金属浅色背景 + 居中展示标题，点击任意空白处缓慢浮现看板 |
| `board.html` | 兼容旧链接：自动跳转到 `index.html#board`（保留查询参数） |
| `reports/<slug>.html` | 每个品牌一篇阅读页（由构建脚本生成，勿手工编辑） |
| `assets/` | 样式与脚本（`site.css` / `board.js` / `home.css` / `home.js` / `report.js`） |
| `data/brands.json` | 唯一数据源：品牌资料与分类定义（可编辑） |
| `data/library.js`、`data/search/*` | 构建生成的浏览器数据与搜索索引（勿手工编辑） |
| `tools/` | 构建、校验、本地服务脚本 |
| `start-site.cmd` | 双击启动本地站点（含编辑模式）并自动打开浏览器 |

## 日常使用

| 目的 | 操作 |
|---|---|
| 本地查看与编辑 | 双击 `start-site.cmd`，浏览器自动打开 <http://127.0.0.1:8787/index.html> |
| 应用编辑接口 | 本地模式下顶部出现「编辑分类」；卡片上出现「编辑资料」 |
| 重新生成站点 | `node tools/build_site.js` |
| 验收检查 | `node tools/verify_site.js`（静态校验 + 浏览器截图到 `site-verify/`） |
| 只跑静态校验 | `node tools/verify_site.js --no-browser` |

## 素材规范（与指南 v4.2 同步；按你的要求先做预保存）

高清要求只针对**品牌标识（LOGO / 封面标识）**，正文插图不设分辨率门槛。

1. 品牌标识以超高清为目标：位图宽度 ≥ 1600 像素，或矢量 SVG。
2. 先取官网素材库、og:image、JSON-LD logo 指向的原文件（去掉 `-750x1000`、`-scaled` 之类的尺寸后缀）。
3. 官网只有压缩小图（如 102×18、170×33 像素）时，用搜索引擎（Google 图片 / Bing 图片）以「品牌名 + logo」检索超高清版本，从官方或权威媒体页面取图，裁掉四周留白后入库。
4. 不得插值放大充数；若全网只有小图，保留官方版本并在台账注明实测尺寸。
5. 替换标识时保持文件名不变或同步正文引用，台账同步更新尺寸与来源。
6. 品牌标识图在报告与网站中**不配图注**（不加 `*图：…*` 说明行），说明性内容只写进台账。

> 注：以上网站素材规则目前只做**预保存**，暂不并入《品牌解读指南》；指南侧的同等条款写在 v4.2 的 12.2 与 12.8 节。

## 展示字体（Erotique）

首页大标题、品牌名与列表/看板中的品牌名称使用展示字体 Erotique。把字体文件（`Erotique.woff2` / `.woff` / `.otf` / `.ttf`）放入 `assets/fonts/`，再运行 `node tools/build_site.js`，构建脚本会自动生成 `assets/fonts.css` 里的 `@font-face`；未放入字体文件时回退到 Bodoni MT / Didot / Georgia 等衬线体（字体已装在系统里时也会自动生效）。

## 内容规则

1. 只收录 `ABOUT_<品牌>_v<版本>.md`；解读指南、撰写规范、示范档案、模板一律不进站点。
2. 每个品牌只显示最新版：取版本号最大的文件，同版本号取文件更新时间最新者；站点不显示版本号，只标注更新时间（即报告文件的最后修改日期）。
3. 报告缺失 Logo 时用衬线体文字封面；`ABOUT_<品牌>_media/` 中的 `Logo_*` 文件会被自动识别为封面。
4. 新出现的报告会自动建档，分类默认「未分类」，在本地编辑模式里指派。
5. 编辑保存直接写入 `data/brands.json`，并立即重建站点数据与阅读页。

## 搜索

- 全局搜索（右上，`/` 或 `Ctrl/⌘ + K` 打开）：结果分「品牌」与「报告内容」两组；内容类结果跳转到阅读页的对应章节，并自动高亮关键词。
- 筛选栏搜索：分类、品牌名称、首字母、更新时间四个下拉各自带有搜索框，只过滤该维度的选项。
- 搜索索引按品牌拆分在 `data/search/`，首次搜索时按需加载。

## 发布到 GitHub Pages

1. `node tools/build_site.js` 后提交并推送到 `main`。
2. 仓库 Settings → Pages → Source 选择 `main` 分支根目录（`/`）。
3. 站点地址形如 `https://<用户名>.github.io/Brand-Asset-Library/`；编辑入口在非本地环境下自动隐藏（只读）。

## 每周一自动更新

已配置每周一 09:00 的 Codex 定时任务：扫描仓库中的报告，发现更新的品牌版本时自动执行构建、校验、提交并推送到 GitHub；无变化时保持安静，仅有待分类新品牌或执行失败时通知。
